apiVersion: loki.grafana.com/v1
kind: LokiStack
metadata:
  name: logging-loki
  namespace: openshift-logging
spec:
  size: 1x.small
  storage:
    schemas:
    - version: v13
      effectiveDate: "2025-11-14"  # Today's date (Nov 14, 2025)
    secret:
      name: logging-loki-azure
      type: azure
  storageClassName: managed-csi
  tenants:
    mode: openshift-logging
